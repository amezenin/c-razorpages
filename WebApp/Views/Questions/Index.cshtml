@model IEnumerable<BLL.App.DTO.Question>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuestionText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionGivesScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionMinScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionMaxScore)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionGivesScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionMinScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionMaxScore)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
