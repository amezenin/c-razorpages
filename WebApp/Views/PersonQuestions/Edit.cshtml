@model PersonQuestionCreateEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PersonQuestion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PersonQuestion.FarmerId" class="control-label"></label>
                <select asp-for="PersonQuestion.FarmerId" class="form-control" asp-items="Model.FarmerSelectList"></select>
                <span asp-validation-for="PersonQuestion.FarmerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PersonQuestion.QuestionId" class="control-label"></label>
                <select asp-for="PersonQuestion.QuestionId" class="form-control" asp-items="Model.QuestionSelectList"></select>
                <span asp-validation-for="PersonQuestion.QuestionId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="PersonQuestion.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
